# MongoShared

These files are shared utilities that are identical across the Cosmos MongoDB and the regular MongoDB vector store providers. They were originally shared via linked compilation in the Semantic Kernel repository.

If a regular MongoDB provider is added in the future, these files should be extracted to a shared location and linked into both projects.
